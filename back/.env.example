# ----------------------
# postgreSQL
# コンテナ名(PostgreSQL)
POSTGRES_NAME=postgres-local

# 初期DB名(PostgreSQL)
POSTGRES_HOST=localhost
POSTGRES_DATABASE=halcinema_postgres_dev

RESTART_POSTGRES_STATE=unless-stopped

# DB情報(PostgreSQL)
POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpassword
POSTGRES_PORT=5432

# ----------------------
# MongoDB
# コンテナ名(mongoDB)
MONGO_NAME=mongo-local

# 初期DB名(mongoDB)
MONGO_DATABASE=halcinema_mongo_dev

RESTART_MONGO_STATE=unless-stopped

# DB情報(mongoDB)
MONGO_USER=devmongo
MONGO_PASSWORD=devpassword
MONGO_PORT=27017

# ----------------------
# File Server
FILE_SERVER_NAME=file-server-local
RESTART_FILE_SERVER_STATE=unless-stopped
FILE_SERVER_PORT=8081
POSTER_STORAGE_PATH=storage/posters
FILE_SERVER_BASE_URL=http://localhost:8081/posters
